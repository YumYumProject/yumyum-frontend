import { useState, useEffect } from "react";

// ดึงข้อมูลจากหลังบ้าน
// รูป menu + menu name + rating of that dish
// how to know which dish is the most popular menu? ใช้เกณฑ์ไหนตัดสิน

const PopMenu = () => {
return (
    
)
}

export default PopMenu